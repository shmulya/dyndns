URL = "http://IP/domain" # server url, like http://127.0.0.1:12227/domain
WORKDIR = "/opt/dyndns-client/" # path to work directory (with / in the end, like /opt/dyndns-client/)
domain = "" # root domain (examlpe.com)
subdomains = [""] # subdomains, like ["first", "second", ...], "" - root domain